// function which does the same but with existing
// elem. params : name of the selector as str +
// data to insert in as a str

function insertElemHTML(selector, data) {
	if (!selector || !data)
		return (-1);
	let nbInserted
	while (let count)
}

// function that apply one function to several 
// event listeners. params : a list of events
// to apply the callback to + the callback.

// function applyOnEvents(events_list, callback) {

// }